diff for task cmqqxalrv000c0jpj83y7moe8

=== src/lib/agent-context.ts ===
Added:
- AgentContextTierItem type (task/wiki/idea/activity/policy with reason, id, title, status, priority, path, url, score, matchedQueries, etc.)
- AgentContextTiers type (hot, warm, cold arrays)
- tiers field on AgentContextPack
- isRecord, asString, asTaskLike helpers for safe runtime narrowing
- isAgentExecutableHotTask, isRecentBlocker predicates
- taskTierItem, wikiTierItem, ideaTierItem, activityTierItem factory functions
- tierKey and pushUnique helpers for deduplication
- buildContextTiers function: current task -> hot; P0/P1 agent_allowed tasks -> hot; recent blockers -> hot; other tasks -> warm; top 3 wiki or score>=30 -> warm; rest wiki -> cold; P0/P1 ideas -> warm; rest ideas -> cold; top 3 activity -> warm; rest -> cold; policy -> cold

=== src/app/api/agent/context/route.ts ===
Changed:
- getQueryAgentContext(query, prisma) now passes prisma so keyword lookups can query the database for hot tasks

=== tests/services/agent-context.test.ts ===
Added:
- "adds P0/P1 agent executable tasks to hot tier for keyword lookups" test
- "puts the current task in hot tier and historical wiki hits in warm tier" test
- assertions for tiers.hot/warm/cold in keyword-only lookup test

=== tests/services/raw-manifest-ingest.test.ts (lint fix) ===
Changed:
- Replaced 4 occurrences of `any` with explicit inline types `{ action: string }` and `{ action: string; reason?: string }`