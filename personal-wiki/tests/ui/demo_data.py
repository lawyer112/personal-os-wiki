"""仅供隔离测试及视觉预览的虚构知识，不加载用户数据。"""
TOPICS = [
 ('知识如何成为可执行的上下文', ['知识管理','智能体'], '让知识参与行动：记录结论、来源和适用条件，把执行所需的章节交给任务，而不是把整座资料库塞给模型。'),
 ('多智能体任务交接规范', ['智能体','协作'], '交接的不只是一个任务标题。当前结论、已完成步骤、运行批次与待解决问题，都应成为下一位执行者可读取的依据。'),
 ('本地模型服务部署清单', ['运维','本地模型'], '从服务健康检查到数据卷与访问凭证，先做可重复的部署检查，再把自动化接入正式流程。'),
 ('把阅读变成可复用的知识', ['阅读','知识管理'], '保留原文来路，用自己的语言整理关键结论，为资料建立少量稳定标签，让下一次检索有迹可循。'),
 ('自动化流程的失败与恢复', ['自动化','运维'], '网络超时不代表操作没有发生。记录请求标识、确认外部状态，让失败可以安全恢复，而不是直接重复执行。'),
 ('项目决策记录的写法', ['项目管理','协作'], '把选择、证据和放弃其他方案的原因写下来。未来回看一项决策时，应当能理解当时的约束。'),
 ('知识库备份与恢复', ['运维','知识管理'], '备份正文、附件和历史版本，并在独立目录验证还原结果。可恢复性比一条备份成功日志更重要。'),
 ('构建个人研究工作流', ['研究','自动化'], '从提出问题、收集证据到记录实验，让每一次研究都有边界，也让下一个问题能够沿着原来的证据继续推进。'),
]

def seed(knowledge):
    notes=[]
    for i,(title,tags,summary) in enumerate(TOPICS):
        references=[TOPICS[(i+1)%len(TOPICS)][0], TOPICS[(i+3)%len(TOPICS)][0], ['结构化知识','版本追溯','执行证据'][i%3]]
        content=f'''## 核心结论

{summary}

> 先明确适用条件，再将这份知识作为下一步行动的依据。

## 为什么要这样做

有用的笔记，不只是对已有信息的复述。它应该解释**结论从哪里来**、在什么条件下适用，以及仍然存在哪些不确定性。

## 操作步骤

1. 明确当前问题和需要交付的结果。
2. 阅读相关来源，记录关键约束与版本。
3. 完成一个可以独立验证的小步骤。
4. 留存证据，并更新下一步建议。

## 验收清单

| 检查项 | 需要留下的记录 |
| --- | --- |
| 知识依据 | 原始来源与使用版本 |
| 执行结果 | 产物、验证记录与限制 |
| 后续动作 | 负责人、下一步与待解决问题 |

- [x] 写明目标与适用范围
- [x] 关联必要的参考知识
- [ ] 在实际使用前再次复核

## 结构化记录示例

```json
{{
  "结果": "保留可核验的交付物",
  "状态": "等待独立复核"
}}
```

## 相关知识

- [[{references[0]}]]
- [[{references[1]}]]
- [[{references[2]}]]

这是一份用于界面验收的虚构示例，不是实际业务操作指令。
'''
        result=knowledge.write_managed_note({'requestId':f'wiki-visual-fixture-{i:03d}','title':title,'content':content,'tags':tags,'metadata':{'space':'个人知识空间','book':'知识与行动手册','chapter':'工作方法' if i%2==0 else '实践记录','owner':'知识维护者','status':'published' if i%3 else 'draft','confidence':'inferred','sensitivity':'internal'}})
        notes.append(result['note'])
    # 形成真正可读取的两个版本，测试页面选择不是静态控件。
    original=notes[0]
    notes[0]=knowledge.write_managed_note({'path':original['path'],'expectedRevision':original['revision'],'title':original['title'],'content':original['content']+'\n\n## 更新记录\n\n补充版本引用和交付证据的说明。','metadata':original['frontmatter']})['note']
    return notes,original
